# `/committed` 頁面 — Inspector 單選/多選整合設計

> 批次選取整併進 Inspector：讓 Inspector 讀起來像是**本來就同時支援單選與多選兩種形態**，而不是在單選編輯面板旁邊另外補一塊批次工具。
>
> 這裡只談互動與 UI 需求，不涉及元件、controller、型別、API。

## 1. 切換單選／多選的地方 — Rail

- 卡片牆（Cards）左側新增一塊**永久固定存在**的直條（rail）。它不是浮動疊層，不隨 Inspector 開關、也不隨卡片牆內部捲動而改變，永遠佔著版面上同一個位置。
- Rail 上是兩個互斥的圖示按鈕：一個代表「單選」、一個代表「多選」，任何時候都可以直接切換，不會因為目前有沒有選取任何東西而消失或不能按。
- 這是使用者切換模式**唯一**的地方，Toolbar 上不再有獨立的「批次選取」開關。

## 2. Inspector 面板的存在時機

- Inspector 面板本身**維持現有邏輯不變**：「有東西可顯示才出現，沒有東西就整塊收合消失」。改變的只是「有東西」在兩種模式下的定義：
  - 單選模式：目前有沒有選中一張圖片正在編輯。
  - 多選模式：目前有沒有勾選任何一張圖片。
- 兩種模式共用同一塊面板區域與寬度，一次只會呈現一種表單。

## 3. 單選 ↔ 多選的銜接

- **單選 → 多選**：切換的當下，單選面正在編輯的那一張圖，自動成為這次多選的起點——自動被視為已選取清單的第一項。是延續使用者剛剛在看的東西，不是憑空冷啟動。
- **多選 → 單選**：不論是按 Inspector 多選面 header 上的關閉圖示，還是直接按 rail 切回單選圖示，都視為**結束這次批次操作**：已勾選的卡片全部清空。下次再進多選，是全新的空選取。

## 4. Inspector 多選面的骨架

刻意跟單選面現有的 header / body 槽位語言對齊，讓兩種表情感覺像同一套設計語言，只是內容不同。

### Header

對齊單選面「檔名 + 第幾張/總數 + 關閉」的結構：

- 左：全選 checkbox，帶文字標籤「全選」（三態：全選／全不選／部分選取，對應目前篩選結果）。
- space-between 之後，中／右：一個 chip，寫「已選取 N 張」（視覺上跟單選面「第幾張 / 總數」那個 chip 同款）。
- 最右：一個關閉圖示按鈕，但語意是「結束批次選取、回到單選模式」——不是單純關掉面板，因為多選模式沒有「只關 Inspector、但還留在多選模式」這種中間狀態。

### Body

- **不設視覺焦點**：單選面 header 之後接的是大圖預覽，是面板最顯眼的視覺重心；多選面刻意不模仿這點，header 之後直接接欄位，不放圖片、縮圖網格，也不放抽象的「N 張」圖示。
- 欄位由上到下：**退回設定 → 評等 → 加標籤 → 去標籤 → 套用**。
- 這四個欄位共用同一套語言：**每個欄位開頭都有一個 checkbox，代表「這次要不要動這個欄位」**，跟欄位本身「要設成什麼值」是分開的兩件事。checkbox 沒勾＝不動，底下的值編輯器（星星／方向選擇／標籤輸入）呈現不可互動的樣子；勾選之後才能編輯。
  - 這樣設計是為了解決評等的一個真實衝突：0 顆星在系統裡本來就是「無評等」這個有意義的值（不是空狀態的視覺殘留），不能拿來兼職代表「不動」。把「要不要動」抽成獨立的 checkbox 之後，星星選擇器就能誠實地代表 0～5，不用再硬拗視覺去區分「未設定」跟「設為無評等」。
  - 加標籤／去標籤的清單本來就有「空清單＝不動」的天然中性狀態，理論上不需要 checkbox；但為了讓整份表單的視覺語言徹底一致（一眼掃過去就知道這次要動哪幾項），四個欄位還是統一都加上同款 checkbox。
  - 這四個欄位的 checkbox／值都只是面板自己暫存的草稿；在按下「套用」之前，不會立即改到任何圖片的既有草稿。
- **退回設定**：底下「標記退回／取消退回」二選一的方向本身，一開始就恆定是「標記退回」——這個值不是 checkbox 勾選後才被指定的預設值，而是從頭就存在、跟 checkbox 有沒有勾完全無關（沒有第三種「未指定方向」的空狀態）。checkbox 沒勾時，這個方向只是先擺在那裡、不會生效；勾選後才會在套用時真正套用「目前選中的方向」。需要取消退回時，使用者自己把方向改成「取消退回」。
- **評等**：checkbox 勾選後，底下是誠實代表 0～5 的星星選擇器；0 顆星＝明確設為無評等，是跟「不動」完全不同、可以主動選中的一個值。
- 「套用」是整份表單唯一的送出動作：一次把**已勾選 checkbox** 的欄位，依照它們目前的值，套用到所有選取圖片的草稿上；沒勾選的欄位完全不受影響。

### 為什麼退回設定是二選一，不是單向按鈕

「退回設定」不能只做成一顆點下去立刻套用的單向按鈕——那樣沒辦法批量「取消」退回。用 body 裡的欄位（checkbox ＋ 標記退回／取消退回二選一，見上）表示，天生就同時支援兩個方向。

### 標記退回時，其他欄位跟著失效

當「退回設定」的 checkbox 勾選、且方向是「標記退回」時，評等／加標籤／去標籤三個欄位要整套變成無法互動——視覺上要跟單選面「一張圖被標記退回後，欄位整套換成唯讀摘要」的失效感一致，因為編輯即將消失的紀錄沒有意義。若方向是「取消退回」，或「退回設定」checkbox 根本沒勾，三者維持正常可互動（這些圖片會留下來，其他欄位的編輯依然有意義）。

## 5. 表單草稿的清空時機

退回設定／評等／加標籤／去標籤這份還沒送出的草稿內容：

- 按下「套用」後清空重置。
- 離開多選模式（回到單選）也會清空重置。
- 但單純調整目前的選取範圍（勾選／取消勾選某張卡片）**不會**清空——可以邊調整這次要套用到哪些圖片，邊保留已經打好的欄位內容。

## 6. 卡片牆不需要額外的批次模式視覺提示

卡片牆不需要額外用特殊背景色／條紋強調批次模式：rail 上的高亮圖示、以及 Inspector 整套換成「沒有圖片、欄位內容完全不同、且套用需要一次明確動作」的多選表情，三者疊加起來已經是足夠強烈的模式提示，不需要卡片牆本身再加任何視覺噪音。

## 7. 尚未收斂

目前沒有還沒收斂的節點。
