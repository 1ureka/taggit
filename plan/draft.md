1. 切換 collection 在 localstorage 紀錄設置過的路徑，可透過上下鍵切換，最多儲存 15 個，並在輸入框下方有提示
2. 改變對 gif 的壓縮方式，簡單來說針對 gif，除非必要，除了最小縮圖型態以外都不再特別壓縮，除非真的過大
3. 新建圖片時可命名

提出後端 JSON 資料庫基礎建設的新架構，使其更容易模組化與局部更新，且須考慮到未來的可測試性，因此比如有關元資料載入、ID 怎樣對應到實際路徑等等或須都得放在不同模組，且各自都能夠獨立運作，讓未來更有可能測試，同時，新的實作要改成實現 Bitmap Index 基礎建設，以及修改對應的 api 入口，並說明前端怎樣實現 facade 搜尋，取消原有的倒排索引機制，因此還得考慮到 ID 與序號的問題，最後是目前 JSON 只儲存圖片ID => 圖片元資料，我希望在不影響舊 db.json 的情況下，也能為標籤本身有元資料的框架，可先初步實現控制標籤是否預設隱藏，預設為不隱藏，所謂隱藏代表只要有該標籤的圖片，在查詢時只要不帶有該標籤，即便有其他標籤命中，也不會出現該圖片，不篩選時自然也是不會出現該圖片

以上都不包括實際代碼修改，你要做得是根據工作量與規劃，寫至少一份 plan/index.md ，並根據情況多寫幾個 md，用於說明可行性與實作規劃

*facade: 標籤篩選應該在當前已篩選後的條件下，顯示仍有的標籤，而不是永遠列出

1. +layout 狀態欄有 defer-pending
2. 切換 collection 改成用檔案總管選取
3. 切換 collection 時，應該要清空快取
4. dirty 時，導航完全沒有要警告的
8. 改善詳細排序能力

10. 實現某種程度的觀看偵測，隨機模式可以嘗試考慮根據次數權重調整
