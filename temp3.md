# `/committed` 頁面 — 互動與 UI 需求

> 這份文件只談使用者會看到什麼、能做什麼、頁面怎麼反應，不涉及 controller 怎麼拆、型別怎麼定義、API 怎麼設計。設計還在收斂中，太早釘死實作細節，之後每次調整互動需求都會變成在既有程式碼上打補丁，而不是讓 controller 保持正交、乾淨——實作留到互動需求穩定後再談。

## 0. 這頁在做什麼

管理已提交圖片：瀏覽、篩選、批次或逐張編輯名稱/標籤/評分，或把圖片退回暫存區重新整理。所有修改先暫存在本地，使用者主動開啟「待提交的變更」清單、逐筆確認後才真正送出，不會邊改邊即時寫入資料庫。

## 1. 進入方式

- 直接從導覽列進入：顯示目前所有已提交圖片。
- 從「瀏覽圖片」首頁的詳情彈窗、或「比較圖片」頁面的卡片，透過各自的「編輯」按鈕直接連過來：進入本頁時沿用來源頁當下的篩選/排序條件，並直接開啟該張圖片的編輯面板（目前首頁那顆按鈕連到不存在的路由，是既有 bug，這裡要一併修正指向本頁）。

## 2. 整體版面

由上而下：篩選/排序列 → 主要內容區（圖片卡片牆 + 可收合的編輯面板）。編輯面板收合時卡片牆佔滿寬度，選取某張圖片後面板從右側滑入。

篩選/排序列的行為要跟「比較圖片」頁面完全一致：關鍵字搜尋圖片名稱、排序（時間／評分／名稱／隨機）與方向、進階篩選（包含/排除的標籤、評分區間）都用同一套元件與互動，讓使用者不用重新學一套操作方式。

## 3. 卡片牆

每張卡片顯示縮圖與精簡資訊（名稱、評分、標籤數）。卡片需要能讓使用者一眼分辨四種狀態：

1. **一般**：目前沒有任何未送出的修改，顯示的是伺服器上的實際資料。
2. **已編輯**：有未送出的欄位修改，用類似「已完成」的視覺標記。
3. **標記退回**：使用者已決定把這張圖退回暫存區，還沒送出。視覺上要跟「已編輯」明顯區分開——這是要讓紀錄消失，不是修改內容。
4. **選取中**（僅在批次選取模式開啟時出現）：勾選狀態的疊層。

點擊卡片的行為依模式而定：一般情況下開啟編輯面板；批次選取模式開啟時改為勾選/取消勾選，不開面板。

## 4. 逐張編輯（編輯面板）

- 面板顯示大圖預覽（可再放大成全螢幕）、名稱、評分、標籤欄位，以及檔名、目前在整批結果中的第幾張。
- 欄位一開始就顯示「目前的實際值」，不是空白——編輯的對象本來就有資料，不是從零開始建立一筆新紀錄。
- 「還原」動作：把這張圖的所有欄位改回伺服器上的實際值（等同放棄這張圖目前的草稿），不是清空成空白。
- 「退回暫存區」／「取消退回」：一個開關動作，標記或取消標記這張圖要被退回。標記退回後，欄位編輯區改成唯讀摘要＋一句提示文字，說明送出後這筆紀錄會消失、檔案回到暫存區。**不需要跳二次確認的對話框**——真正的動作要等到審查清單裡明確送出才會發生，審查清單本身就是確認的關卡，這裡再加一層確認反而是多餘的摩擦。
- 關閉編輯面板不等於放棄修改：草稿會保留，卡片牆上該卡片會維持「已編輯」或「標記退回」的狀態，直到使用者主動還原、或整批送出。
- 上一張／下一張：在目前篩選結果內導覽；如果目前選取的圖片因為篩選/排序條件改變而不在可視範圍內了，面板自動關閉。

## 5. 批次編輯

大量圖片需要同時做同一件事（例如「篩選出這 80 張，全部加上一個標籤」）時，逐張點進編輯面板不切實際，因此需要一個獨立於逐張編輯的批次流程：

- Toolbar 上有一顆「批次選取」切換鈕。開啟後：
  - 卡片點擊行為改成勾選，不開編輯面板（見第 3 節）。
  - 卡片牆**上方**多出一個常駐的列，這個列跟卡片牆屬於同一塊版面裡的兄弟元素，不是疊加在卡片牆上面的浮動層，也不會跟著卡片牆內部捲動一起捲走——不管卡片牆捲到哪裡，這個列都維持在原位可見。它的寬度就是卡片牆本身的寬度；編輯面板同時開著時，這個列會跟卡片牆一起變窄，但即使變窄也還是比單純一顆浮動小徽章寬得多。
  - 因為這個列本身已經很顯眼，卡片牆**不需要**額外用特殊背景色或條紋去強調「目前在批次選取模式」（不同於暫存區圖章模式整片條紋背景的既有做法）；卡片本身「選取中」的疊層（checkbox、強調色邊框）不受影響，維持不變。

- 這個列由左到右安排：
  - 全選 checkbox（三態：全選／全不選／部分選取，對應目前篩選結果），旁邊接著「已選取 N 張」文字——這是批次操作最主要的使用情境：先篩選縮小範圍，再一鍵全選整批套用。
  - 三個動作觸發鈕，外觀統一是「文字＋一個向下箭頭圖示」，樣子完全一致：`[新增標籤 ⌄]  [去除標籤 ⌄]  [評等 ⌄]`。這三顆觸發鈕本身**不會**因為使用過而改變外觀（不會變成顯示已套用的內容、不會有數量徽章），永遠都是同樣的樣子——它們是「動作啟動器」，不是「目前狀態的顯示」。
    - **評等**：點下去彈出一個選單，選項是 1～5 星，每一個選項前面都要有一顆星星圖示（例如「★ 3」）。點選任一個選項，立刻把該評等套用到所有選取圖片的草稿，選單隨之收合，不需要額外確認。
    - **加標籤／去標籤**：點下去彈出一個面板，裡面是標籤輸入區（跟站內其他地方的標籤輸入互動一致：可以打字搜尋候選、已輸入的標籤以 chip 呈現、可以再移除其中一個；chip 清單堆疊在輸入框**下方**，不是上方，讓輸入框位置固定不動，方便連續輸入），下方多一顆「套用」按鈕。**在面板裡打字、新增、移除標籤，都只是面板自己暫時記著的內容，不會立即改到任何圖片**；只有按下「套用」，才把當下面板裡的整份標籤清單一次套用到所有選取圖片的草稿，面板隨之關閉。如果沒有按套用就把面板關掉（例如點了面板外部），裡面打好的標籤直接作廢，不會留到下次打開。
      - 「加標籤」可以打任何標籤名稱，包含全新的標籤，跟一般標籤輸入的行為一致。
      - 「去標籤」限定只能挑「這次選取的圖片中，至少有一張目前真的有」的標籤——避免使用者對著空氣操作、或者打錯字。
  - **標記退回**：一顆獨立的按鈕（跟前面三個下拉樣式的觸發鈕不同，就是一顆普通按鈕），點下去立刻把所有選取圖片標記為退回，不需要額外確認（原因同第 4 節：真正的確認關卡在最後的審查清單，這裡再加一層確認是多餘的摩擦）。
  - 最右側是「結束批次選取」：清空已選取的項目，並整個離開批次選取模式，卡片點擊行為改回開啟編輯面板，這個列也隨之收起。

- 批次動作套用後**不會立即送出**，全部只是寫進每張圖各自的草稿。批次選取模式開啟期間，點卡片是勾選而不是開面板，若要針對某一張再個別微調，需要先結束批次選取。
- 批次動作跟逐張編輯共用同一份草稿狀態，沒有各自獨立的資料——批次只是「一次對多張圖片重複同一個編輯動作」的捷徑，不是另一套機制。

## 6. 送出前的審查清單

點「檢視待提交的變更」開啟審查清單，列出所有有未送出草稿（含標記退回）的圖片：

- 清單頂部有全選 checkbox（三態：全選/全不選/部分選取），讓使用者最後決定這次真的要送出哪些。
- 每一列依草稿種類呈現不同內容：
  - **一般編輯**：名稱、評分只在「真的有改」時才顯示「原本 → 現在」的對比，沒改的欄位不顯示，避免每列都塞滿雜訊。標籤異動用新增（綠色 +）／移除（紅色 −）的視覺區分清楚呈現，不是單純列出最終的標籤清單——這樣使用者才看得出「這次動作實際改了什麼」，而不只是「改完長怎樣」。單一方向的異動標籤太多時，收斂成一個「+N」／「−N」的概略標示，避免整列被塞爆。
  - **標記退回**：用一個顯眼的徽章標出「退回」，本體用刪除線呈現即將被清空的名稱/評分/標籤摘要（因為送出後這筆紀錄整個消失，沒有「之後」可比較）。
- 每一列都可以個別取消勾選（排除在這次送出之外），也可以直接點縮圖看大圖、點名稱回到編輯面板繼續調整。
- 清單底部要說明「這次送出對標籤庫整體的影響」，且**必須同時涵蓋新增與移除兩個方向**，不能只講會新增什麼：
  - 開啟清單、或使用者變動勾選狀態時，這段文字要**立刻**（不能有肉眼可感覺到的延遲）轉成讀取中的 skeleton 佔位樣式（不是轉圈圈+文字，直接是這個區塊自己的 CSS 樣式，不需要另外做一個共用元件），讓使用者馬上感覺到系統有反應。
  - 真正送出查詢的時機才 debounce（短暫等待使用者的勾選動作告一段落，避免連續勾選好幾個 checkbox 時每點一次就各自打一次 API）——這個 debounce 只影響「什麼時候真正發送查詢」，跟 skeleton 什麼時候出現是兩件事，不能因為要 debounce 查詢，連帶讓 skeleton 也跟著延遲出現。
  - 查詢完成後只會是以下其中一種情況：會新增幾個全新的標籤、會讓幾個標籤變成完全沒有圖片在用、兩者都有、或者完全不會影響標籤庫（勾選狀態不同，結果也會跟著不同）。
  - 新增/移除一樣用綠 + / 紅 − 的視覺語言，跟每列的標籤呈現手法一致。
- 送出按鈕清楚標示這次會送出幾張；送出中要能看出正在處理，避免使用者重複點擊；送出後失敗的項目要個別標示原因（例如：這張圖在你編輯的期間被其他操作改過，需要重新整理），不會因為部分失敗就整批回滾。

## 7. 離開頁面

有未送出的草稿（含標記退回）時，不管是關掉分頁、重新整理、還是點連結離開，都要提示使用者將會遺失這些修改，讓使用者可以選擇留下。

## 8. 明確不做

- 暫存區那種「釘選一張當範本、拖曳塗抹到其他卡片」的圖章模式——第 5 節的「篩選 + 全選 + 批次動作」已經涵蓋同樣的需求，且更適合這頁常見的大範圍批次情境，兩者不並存。
> 暫存 (/staged) 在新的批量模式(內部名稱，絕不應該在 UI 寫出 "批量" 與 "模式" 兩種不友善的詞)設計後，圖章模式也會刪除，在之後 staged 重寫時也會改成與 committed 類似

## 9. 還沒收斂的小節點

目前沒有還沒收斂的節點。
