# Taggit v3

**使用任何 `$lib/components` 的組件之前，絕對要先閱讀 `/lab` 路由（`src/routes/lab/`）中對應的 use case 展示頁。**

組件本身的 props 型別看不出使用慣例——例如 TextInput 的 helper text 該怎麼寫，只看 `TextInput.svelte` 是看不出來的，必須看 `lab/(showcase)/(inputs)/type-*` 的實際用法。在完全遷移完成之前，此準則恆為真。
